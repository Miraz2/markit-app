# AGENTS.md

## Build / Verification
- Do NOT run `npm run build` after every change. Only build when the user explicitly asks.

## Git
- Always use relevant, descriptive commit messages (never generic ones like "first commitN").
