apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - 00-namespace.yaml
  - 01-configmap.yaml
  - 03-backend.yaml
  - 04-frontend.yaml
  - 05-minio.yaml
  - 06-rabbitmq.yaml
# NOTE: 02-secrets.yaml is intentionally NOT included here.
# Apply it separately once you've created it with real values:
#   kubectl apply -f k8s/base/02-secrets.yaml
